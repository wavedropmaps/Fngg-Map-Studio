javascript:(function () { var VERSIONS = [ ['38.00', 'jpg', 'Springfield (launch)'], ['38.01', 'jpg', 'Springfield (mid)'], ['38.11', 'jpg', 'Springfield (late)'], ['37.52', 'jpg', 'pre-Simpsons'], ['41.20', 'webp', 'current (undo)'] ]; var RE = /\/maps\/[\d.]+\/(\d+)\/(-?\d+)\/(-?\d+)\.(?:jpg|webp)/; var active = null; function swap(img) { if (!active || !img || !img.src) return; var m = img.src.match(RE); if (!m) return; var url = 'https://fortnite.gg/maps/' + active[0] + '/' + m[1] + '/' + m[2] + '/' + m[3] + '.' + active[1]; if (img.src !== url) img.src = url; } function apply(v) { active = v; try { localStorage.setItem('fnggSwapVersion', v[0]); } catch (e) {} var n = 0; document.querySelectorAll('img').forEach(function (i) { var before = i.src; swap(i); if (i.src !== before) n++; }); if (!window.__fnggObs) { window.__fnggObs = new MutationObserver(function (muts) { muts.forEach(function (mu) { if (mu.type === 'attributes') { swap(mu.target); return; } mu.addedNodes.forEach(function (nd) { if (nd.tagName === 'IMG') swap(nd); else if (nd.querySelectorAll) nd.querySelectorAll('img').forEach(swap); }); }); }); window.__fnggObs.observe(document.body, { childList: true, subtree: true, attributes: true, attributeFilter: ['src'] }); } render(); return n; } var box; function render() { if (!box) { box = document.createElement('div'); box.style.cssText = 'position:fixed;top:60px;left:12px;z-index:99999;background:#1c1c1c;' + 'color:#eee;border:1px solid #555;border-radius:6px;padding:10px;font:13px sans-serif;' + 'box-shadow:0 4px 16px rgba(0,0,0,.5);min-width:190px'; document.body.appendChild(box); } box.textContent = ''; var h = document.createElement('div'); h.textContent = 'Map version'; h.style.cssText = 'font-weight:700;margin-bottom:8px'; box.appendChild(h); VERSIONS.forEach(function (v) { var b = document.createElement('button'); b.textContent = v[0] + ' ' + v[2]; var on = active && active[0] === v[0]; b.style.cssText = 'display:block;width:100%;text-align:left;margin:3px 0;padding:5px 8px;' + 'border-radius:4px;cursor:pointer;border:1px solid ' + (on ? '#fce51e' : '#444') + ';background:' + (on ? '#3a3a1c' : '#2a2a2a') + ';color:' + (on ? '#fce51e' : '#eee'); b.onclick = function () { apply(v); }; box.appendChild(b); }); var x = document.createElement('button'); x.textContent = 'close'; x.style.cssText = 'margin-top:8px;padding:4px 8px;border-radius:4px;border:1px solid #444;' + 'background:#2a2a2a;color:#999;cursor:pointer;width:100%'; x.onclick = function () { box.remove(); box = null; }; box.appendChild(x); } var saved = null; try { saved = localStorage.getItem('fnggSwapVersion'); } catch (e) {} var match = VERSIONS.filter(function (v) { return v[0] === saved; })[0]; if (match) apply(match); else render(); })();